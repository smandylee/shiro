# 작업 로그

컴퓨터를 옮겨가며 작업하니까, **다른 데서 이어서 시작할 때 여기부터 읽는다.**
`README.md`는 "어떻게 돌리나"(설치·배포·파이프라인·함정)를 다루고,
이 파일은 "어디까지 했고 다음에 뭘 하나"만 다룬다. 내용이 겹치면 README가 정답이다.

작업을 마치면 **[상태]와 [다음]을 고치고, [기록] 맨 위에 한 줄 남긴 다음 커밋·푸시한다.**
푸시를 안 하면 다른 컴퓨터에서는 이 파일이 없는 것과 같다.

---

## 상태 (2026-09-08 기준)

동작하는 것:

- **orchestrator** — VM에서 `shiro-orchestrator.service`로 24/7 구동 중.
  디스코드 DM 대화, 도구 22개(주인용) / 2개(손님용), Gmail·캘린더·드라이브,
  단기기억(SQLite) + 장기기억(Pinecone), 셸 명령 제안→승인→실행, 일정 리마인더.
- **avatar** — Electron 투명 오버레이. 21레이어 합성, 꼬리 벤딩(중심선 24마디 체인),
  머리 스프링 추종, 머리카락 지연, 눈 깜빡임, 시선 이동, 감정 8종 포즈 전환.
- **브릿지** — WebSocket(:18790) 토큰 인증, 하트비트, 자동 재연결.

즉, **"감정을 몸으로 표현하는" 부분까지는 끝났고, "얼굴로 표현하는" 부분이 통째로 남았다.**

---

## 다음 (우선순위 순)

### 1. 표정을 렌더러에 실제로 연결하기 ← 지금 여기

지금 감정 표현은 꼬리·귀·고개뿐이다. 얼굴은 깜빡임과 시선 말고는 아무것도 안 움직인다.

- 재료는 이미 있다: `avatar/tools/preview-face.mjs:38-44` 에
  감정 8종 × `brow / raise / eye / pupil / blush / mouth` 테이블이 완성돼 있다.
- 없는 것: `avatar/renderer/emotions.js` 의 `BASE` 에 눈·동공·볼터치·입 필드가 없고,
  `avatar/renderer/app.js` 의 `applyGroup()` 에 그걸 적용하는 case가 없다.
- 할 일: 그 파라미터들을 `BASE`/`EMOTION_POSES` 로 옮기고(그래야 `blendPose`가 보간해준다),
  `applyGroup()` 의 `eyes` / `irides` / `mouth` case에서 스케일로 적용한다.
- 주의: **눈썹은 못 쓴다.** 앞머리가 완전히 덮어서 22도로 꺾어도 화면상 차이가 없다.
  `preview-face.mjs` 의 `brow` 값은 참고만 하고 실제로는 눈 크기·동공·볼터치·입으로 만든다.
- 볼터치 레이어는 아직 없다. 캔버스에 직접 그리거나(반투명 타원) 레이어를 새로 만들어야 한다.

### 2. 표정용 입 이미지 4종 만들기

지금 `avatar/layers/mouth.png` 는 "벌린 미소" 하나뿐. 1번을 해도 입은 크기만 변한다.

- NovelAI **인페인팅**으로 뽑는다. 새로 생성하면 얼굴이 어긋나서 안 된다.
- 베이스 `avatar/shiro_base.png` (1536×1536), 마스크 `avatar/mouth-mask.png` — 둘 다 이미 있다.
- 필요한 모양: 닫은 입 / 작은 o / 시무룩 / (선택) 물결.
- 1536 → 1280 은 정확히 6:5 단순 리사이즈라 좌표가 그대로 대응된다.
  See-through를 다시 돌릴 필요 없다.

### 3. 창 크기 / 설정 저장

- 창은 `avatar/main.js` 에서 600×620 고정인데 조작 패널의 크기 슬라이더는 max 0.8까지 간다.
  `(1280+340)*0.8 = 1296px` 이라 **0.37을 넘기면 그림이 창에 잘린다.**
  슬라이더에 맞춰 창을 키우든지, 슬라이더 상한을 창에 맞추든지 하나로 정해야 한다.
- 슬라이더·말풍선 토글이 메모리에만 남는다. `config.json` 에 저장해서 재시작해도 유지되게.

### 4. 목소리 (TTS + 립싱크)

`orchestrator/src/avatar/bridge.ts:27` 의 `hasAvatarClient()` 는
"아바타가 붙어 있을 때만 TTS를 켠다"는 주석만 달린 채 아무 데서도 안 쓰인다. 자리만 잡아둔 상태.
말할 때 입이 움직이지도 않는다. 2번(입 이미지)이 끝나야 제대로 할 수 있다.

### 자잘한 것

- `orchestrator/src/llm/gemini.ts` 의 `runTool()` 이 `sessionKey` 를 받기만 하고 안 쓴다.
- 브릿지 바인딩 방식이 README(`AVATAR_BRIDGE_HOST` 에 tailscale IP)와
  `bridge.ts` 주석(`tailscale serve` 권장)이 서로 다르게 말한다. 하나로 통일.
- 손님과의 대화도 감정이 주인님 아바타에 그대로 반영된다
  (`index.ts` 가 `isOwner` 와 무관하게 `avatarSay` 호출). 말풍선은 기본 off라 내용은 안 보인다.
  의도한 동작인지 정하고 필요하면 막는다.
- `avatar/layers/earwear.png`, `neckwear.png` 는 빈 레이어라 `layers.json` 에 안 들어가 있다.
  버그 아님 — `build-layers.mjs` 가 200px 미만이면 버리는데, 파일 자체는 남긴다. 무시해도 된다.

---

## 기록

최신이 위.

### 2026-09-08 — 코드 전체 분석, 이 파일 생성

다른 컴퓨터에서 이어서 작업할 수 있게 현황을 정리했다. 코드 변경 없음.
위의 [다음] 목록이 이 분석의 결과다. 특히 **표정이 프리뷰 툴에만 있고 실물 렌더러에는
연결돼 있지 않다**는 게 가장 큰 구멍.

### 2026-08-28 — 최초 커밋 (`d487ac9`)

orchestrator(디스코드·Gemini·기억·구글 연동)와 avatar(21레이어 합성·꼬리 벤딩·감정 포즈),
그리고 둘을 잇는 WebSocket 브릿지까지 한 번에.
