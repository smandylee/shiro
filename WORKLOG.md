# 작업 로그

컴퓨터를 옮겨가며 작업하니까, **다른 데서 이어서 시작할 때 여기부터 읽는다.**
`README.md`는 "어떻게 돌리나"(설치·배포·파이프라인·함정)를 다루고,
이 파일은 "어디까지 했고 다음에 뭘 하나"만 다룬다. 내용이 겹치면 README가 정답이다.

작업을 마치면 **[상태]와 [다음]을 고치고, [기록] 맨 위에 한 줄 남긴 다음 커밋·푸시한다.**
푸시를 안 하면 다른 컴퓨터에서는 이 파일이 없는 것과 같다.

---

## 상태 (2026-09-25 기준)

동작하는 것:

- **orchestrator** — VM에서 `shiro-orchestrator.service`로 24/7 구동 중.
  디스코드 DM 대화, Gmail·캘린더·드라이브, 단기기억(SQLite) + 장기기억(Pinecone),
  셸 명령 제안→승인→실행, 일정 리마인더.
  - **검색은 도구로 분리**됐다. 매 턴 그라운딩하지 않고 필요할 때만 `web_search`.
  - **화면을 본다** — 캡처 요청, 게임 관전 모드.
  - **먼저 말을 건다** — 궁금한 것을 모아뒀다가 묻는다. 밤에도 묻는다.
    직전 질문에 답을 안 했으면 더 묻지 않는다.
  - **프로필을 쌓는다** — 대화에서 주인님에 대한 사실을 뽑아 저장, 스냅샷으로 되돌릴 수 있다.
  - **취업 공고** — PC의 JobSpy 크롤러가 모아둔 홍콩 공고를 물어볼 때만 준다.
  - **개발 요청** — 시로가 말로 요청 → 주인님 승인 → PC의 Claude Code가 격리된 worktree에서 작업.
    판정이 완료/반려/**넘김**(권한 밖이라 주인님이 직접) 셋으로 온다.
  - **배포** — 결과가 마음에 들면 "배포해" 한 마디로 서버에 올라간다.
    합치기→타입체크→업로드→재시작→헬스체크, 실패하면 서버와 main 둘 다 자동 롤백.
- **avatar** — Electron 투명 오버레이. 21레이어 합성, 꼬리 벤딩(중심선 24마디 체인),
  머리 스프링 추종, 머리카락 지연, 눈 깜빡임, 시선 이동, 감정 8종 포즈 전환.
  집어서 옮기기, 크기 조절, 마이크 입력(푸시투토크, 3.5~5초).
  **목소리는 붙었다** — ElevenLabs 스트리밍 TTS + 음량 분석으로 입 움직임.
- **브릿지** — WebSocket(:18790) 토큰 인증, 하트비트, 자동 재연결.
  개발 요청은 아바타와 **별도 워커**(`avatar/devworker.js`)가 받으므로 아바타를 꺼도 된다.
- **minecraft** — 1단계 완료, 2단계 절반. Paper 26.1.2 서버(로컬 전용)에 mineflayer 봇이 붙는다.
  반사신경(먹기·도망·반격·밤 감지)과 기술(이동·캐기·제작·도구)이 있고 채팅으로 시켜볼 수 있다.
  **아직 스스로 목표를 정하지 않는다.** 그리고 **밤에 계속 죽는다** — 아래 [다음] 참고.

즉, **몸·목소리·손(개발·배포)까지 왔고, 얼굴 표정이 여전히 통째로 남았다.**

---

## 다음 (우선순위 순)

### ★ 시로 마인크래프트 — 2단계 마무리: 밤을 넘기기 ← 지금 여기

설계 결정·구조·지킬 것·진행 순서는 전부 **`docs/minecraft.md`** 에 있다. 거기부터 읽는다.
돌리는 법과 26.1 에서 걸린 것들은 `minecraft/README.md`.

1단계는 끝났고, 2단계의 반사신경·기술도 코드로는 다 있다. **막힌 건 하나다: 밤에 죽는다.**

낮에는 나무를 캐고 판자·작업대를 만드는 것까지 확인했다. 그런데 밤이 되면 크리퍼·좀비에게
죽어서 가진 것을 전부 떨어뜨리고, 그래서 곡괭이까지 가는 사슬이 한 번도 안 이어졌다.
돌은 곡괭이가 없으면 캐도 안 나오므로 **나무→판자→작업대→곡괭이→돌** 이 순서가 전부 막힌다.

전투 정책은 두 번 고쳤다. 처음엔 12칸 안 좀비에게 맨손으로 달려들어 2분에 세 번 죽었고,
반대로 고치니 이번엔 12칸 밖 스켈레톤에게도 6초마다 도망쳐서 아무것도 못 끝냈다.
지금은 "가까이 왔거나(3.5칸) 방금 맞았을 때만" 반응한다. 그래도 크리퍼는 터진다.

다음에 할 것 (우선순위 순):
1. **밤을 넘기는 법.** 침대가 없으면 못 잔다. 표준 해법은 땅을 파고 들어가 막는 것 —
   `밤 + 침대 없음 + 위협` 이면 2칸 파고 블록으로 막는 반사신경을 만든다.
2. 안전해지면 곡괭이까지 사슬을 이어서 확인한다.
3. 그 다음이 3단계(Gemini 계획자).

3단계 전에 정할 것:
- **LLM 호출 시간당 상한.** 개발 요청은 Max 구독 안이라 청구가 없었지만
  **시로의 Gemini 호출은 Vertex 실과금**이다. 기존 `usage_daily` 는 일 단위라 시간 단위를 얹어야 한다.
- **게임 채팅 감정을 아바타에 반영할지.** 지금은 손님 대화 감정도 그대로 아바타로 간다.
- `prismarine-viewer`(시로 시점 보기)는 아직 안 붙였다. 26.1 지원이 2026-09-21 에 머지됐으니
  npm 에 없으면 git 에서 설치해야 한다.

### 1. 표정을 렌더러에 실제로 연결하기

아래 1~3번은 2026-09-08 분석에서 남은 것이다. 그 뒤 커밋으로 끝난 항목(TTS·립싱크,
위치·크기 저장)은 이미 정리해서 표시해뒀다.

지금 감정 표현은 꼬리·귀·고개뿐이다. 얼굴은 깜빡임과 시선 말고는 아무것도 안 움직인다.

- 재료는 이미 있다: `avatar/tools/preview-face.mjs:38-44` 에
  감정 8종 × `brow / raise / eye / pupil / blush / mouth` 테이블이 완성돼 있다.
- 없는 것: `avatar/renderer/emotions.js` 의 `BASE` 에 눈·동공·볼터치·입 필드가 없고,
  `avatar/renderer/app.js` 의 `applyGroup()` 에 그걸 적용하는 case가 없다.
- 할 일: 그 파라미터들을 `BASE`/`EMOTION_POSES` 로 옮기고(그래야 `blendPose`가 보간해준다),
  `applyGroup()` 의 `eyes` / `irides` / `mouth` case에서 스케일로 적용한다.
- 주의: **눈썹은 못 쓴다.** 앞머리가 완전히 덮어서 22도로 꺾어도 화면상 차이가 없다.
  `preview-face.mjs` 의 `brow` 값은 참고만 하고 실제로는 눈 크기·동공·볼터치·입으로 만든다.
- 볼터치 레이어는 아직 없다. 캔버스에 직접 그리거나(반투명 타원) 레이어를 새로 만들어야 한다.

### 2. 표정용 입 이미지 4종 만들기

지금 `avatar/layers/mouth.png` 는 "벌린 미소" 하나뿐. 1번을 해도 입은 크기만 변한다.

- NovelAI **인페인팅**으로 뽑는다. 새로 생성하면 얼굴이 어긋나서 안 된다.
- 베이스 `avatar/shiro_base.png` (1536×1536), 마스크 `avatar/mouth-mask.png` — 둘 다 이미 있다.
- 필요한 모양: 닫은 입 / 작은 o / 시무룩 / (선택) 물결.
- 1536 → 1280 은 정확히 6:5 단순 리사이즈라 좌표가 그대로 대응된다.
  See-through를 다시 돌릴 필요 없다.

### 3. 창 크기 / 설정 저장

- 창은 `avatar/main.js` 에서 600×620 고정인데 조작 패널의 크기 슬라이더는 max 0.8까지 간다.
  `(1280+340)*0.8 = 1296px` 이라 **0.37을 넘기면 그림이 창에 잘린다.**
  슬라이더에 맞춰 창을 키우든지, 슬라이더 상한을 창에 맞추든지 하나로 정해야 한다.
- 위치와 크기는 `avatar/.window-state.json` 에 저장된다. **말풍선 토글은 아직 메모리에만** 남는다.

### 4. ~~목소리 (TTS + 립싱크)~~ — 끝났다

ElevenLabs 스트리밍 TTS 가 붙었고, 음량을 분석해서 입이 움직인다.
다만 입 모양이 하나뿐이라 **크기만 변한다** — 2번(입 이미지 4종)을 하면 제대로 된다.

### 자잘한 것

- `orchestrator/src/llm/gemini.ts` 의 `runTool()` 이 `sessionKey` 를 받기만 하고 안 쓴다.
- 브릿지 바인딩 방식이 README(`AVATAR_BRIDGE_HOST` 에 tailscale IP)와
  `bridge.ts` 주석(`tailscale serve` 권장)이 서로 다르게 말한다. 하나로 통일.
- 손님과의 대화도 감정이 주인님 아바타에 그대로 반영된다
  (`index.ts` 가 `isOwner` 와 무관하게 `avatarSay` 호출). 말풍선은 기본 off라 내용은 안 보인다.
  의도한 동작인지 정하고 필요하면 막는다.
- `avatar/layers/earwear.png`, `neckwear.png` 는 빈 레이어라 `layers.json` 에 안 들어가 있다.
  버그 아님 — `build-layers.mjs` 가 200px 미만이면 버리는데, 파일 자체는 남긴다. 무시해도 된다.

---

## 기록

최신이 위.

### 2026-09-25 — 마인크래프트 2단계: 반사신경과 기술

`reflexes.js`(먹기·도망·반격·밤 감지)와 `skills.js`(이동·캐기·제작·도구)를 만들고
`bot.js` 에 붙였다. 계획자가 없으니 게임 채팅으로 기술을 하나씩 시켜서 시험했다.

**플러그인은 깔리는 것과 도는 것이 다르다.** 넷 다 26.1 에서 로드는 되는데:
- `collectblock` 은 2칸 거리 블록에서도 시간 초과하고, 성공을 반환해도 아이템이 안 들어온다.
  → **안 쓴다.** 이동→도구→`dig`→드롭 줍기를 직접 짰다. 그 셋은 각각 멀쩡하다.
- 인벤토리 2×2 에서 **일부만 채우는 레시피가 깨진다.** 막대기(판자 2개)를 시켰더니 판자를
  하나만 놓아서 서버가 **버튼**을 만들었다. → 꽉 채우거나 모양 없는 레시피만 거기서 만들고
  나머지는 작업대를 쓴다.
- `pathfinder`, `auto-eat` 은 그대로 잘 된다.

**내가 만든 버그 둘.** `craft()` 가 작업대를 찾으러 `craftingTable()` 을 부르고 그게 다시
`craft("crafting_table")` 을 불러서 재료가 없으면 스택이 터질 때까지 돌았다. 그리고 재접속마다
반사신경을 새로 설치해서 죽은 봇의 타이머가 계속 쌓였다.

**봇이 둘 돌면 서로 쫓아낸다.** 같은 이름으로 접속하면 서버가 먼저 있던 쪽을 내보내고,
그쪽이 재접속하며 상대를 내보낸다. `.bot.lock` 으로 두 번째 실행을 막았다.

**남은 것은 밤이다.** 자세한 건 [다음] 참고.

### 2026-09-25 — 마인크래프트 1단계, 그리고 그동안 밀린 기록

**마크 1단계 끝.** Paper 26.1.2(빌드 74) 서버가 로컬 전용(`127.0.0.1`)으로 돌고,
mineflayer 봇이 접속해서 채팅을 주고받는다. 손님 봇으로 말을 걸어 왕복까지 확인했다.

정한 것과 이유는 `minecraft/README.md` 에 있다. 요점만:
- **26.1.2 + 프로토콜 775.** mineflayer 가 26.1 까지만 지원하고, 26.1/26.1.1/26.1.2 는
  프로토콜이 같아서 서버는 26.1.2, 봇은 `version: "26.1"` 고정으로 맞춘다.
- **Java 25 를 `minecraft/jdk/` 안에만** 풀었다. 이 PC 의 Java 17 과 `JAVA_HOME` 은 안 건드린다.
- `enforce-secure-profile=false` 가 필요했다. 오프라인 모드에서 채팅 서명이 봇을 막는다.

**이 파일이 2주 넘게 안 맞았다.** 9/8 이후에 들어간 것들이 통째로 빠져 있었다 —
검색 도구 분리, 화면 캡처·관전, 음성 입력, 프로필 학습, 먼저 질문하기, 취업 공고(JobSpy),
개발 요청, 배포 경로, TTS·립싱크, 아바타 드래그·크기 조절. [상태]에 반영했다.
**단계 끝날 때마다 여기를 고치고 푸시한다** — 안 그러면 이렇게 된다.

### 2026-09-25 — 마인크래프트 설계, `docs/minecraft.md` 추가

주인님과 논의해서 시로 마크의 방향을 정했다: Java 바닐라 26.1 계열, 오프라인 모드 로컬 Paper 서버,
mineflayer 워커는 PC에, 판단(Gemini)은 VM에. 코드 변경 없음. 구현은 마크 전용 PC에서 시작한다.

### 2026-09-08 — 코드 전체 분석, 이 파일 생성

다른 컴퓨터에서 이어서 작업할 수 있게 현황을 정리했다. 코드 변경 없음.
위의 [다음] 목록이 이 분석의 결과다. 특히 **표정이 프리뷰 툴에만 있고 실물 렌더러에는
연결돼 있지 않다**는 게 가장 큰 구멍.

### 2026-08-28 — 최초 커밋 (`d487ac9`)

orchestrator(디스코드·Gemini·기억·구글 연동)와 avatar(21레이어 합성·꼬리 벤딩·감정 포즈),
그리고 둘을 잇는 WebSocket 브릿지까지 한 번에.
